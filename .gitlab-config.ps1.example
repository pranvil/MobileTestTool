# GitLab 配置文件示例
# 复制此文件为 .gitlab-config.ps1 并填写您的实际配置
# 注意：.gitlab-config.ps1 已在 .gitignore 中，不会被提交到 Git

# GitLab 服务器地址
$GitLabUrl = "http://10.129.93.67"

# GitLab 项目所有者（用户名或组名）
$GitLabOwner = "hao.lin"

# GitLab 项目名称
$GitLabRepo = "mobiletesttool"

# GitLab Access Token（从 GitLab 设置中生成）
# 需要权限：api, write_repository
$GitLabToken = "your_token_here"

